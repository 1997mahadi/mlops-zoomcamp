[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
scikit-learn = "==1.4.2"  # Update to match the version required by MLflow
flask = "*"
gunicorn = "*"
mlflow = "==2.13.0"  # Update to match the version required by MLflow
boto3 = "*"  # You can keep this as wildcard if there are no specific version requirements
cloudpickle = "==2.2.1"
packaging = "==21.3"
psutil = "==5.8.0"
scipy = "==1.13.0"

[dev-packages]
ll = "*"
requests = "*"

[requires]
python_version = "3.9"
